version: "2"

services:
  blocklator:
    container_name: blocklator
    image: tinywell/blocklator:latest
    tty: true
    stdin_open: true
    # environment:
    #
    ports:
      - 8080:80
    working_dir: /app
    # command:
    # volumes:
